apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-green-django-app-green
spec:
  rules:
    - host: django.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: django-green-django-app-green
                port:
                  number: 8000
